#include <iostream>
#include <math.h>
using namespace std;
int main () {
double a,b,c,d;
cout<<"Vuvedi 3 chisla\n";
cin>>a;
cin>>b;
cin>>c;
d=pow(a,2)+pow(b,2)+pow(c,2);
cout<<sqrt(d/3);
return 0;
}

